"use client";

import { FcGoogle } from "react-icons/fc";
import BorderedOrangeButton from "../Buttons/BorderedOrangeButton";
import { useFormState } from "react-dom";

const SignInWithGoogle = () => {
  //     const [state, signInWithGoogleActionForm] = useFormState(signInWithGoogleAction, {});
  //   return (
  //     <form action={signInWithGoogleActionForm}>
  //       <BorderedOrangeButton full>
  //         <div className="flex items-center gap-3">
  //           <FcGoogle size={20} />
  //           Sign in with Google
  //         </div>
  //       </BorderedOrangeButton>
  //     </form>
  //   );
  return <></>;
};

export default SignInWithGoogle;
